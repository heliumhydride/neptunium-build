dl_build_install.sh:
  add quotes arround "$BUILD_JOBS"
  double check every path
neptunium base files release
test build for amd64
general code cleanup
update download urls for packages (notably w64devkit to 2.0)
build_vim: check if running on windows, if so then set UNDER_CYGWIN=no
test build for x86 and arm64
add armv7 support
